[tool.poetry]
name = "my_local_package"
version = "0.1.0"
description = "A small local package"
authors = ["lilian-delouvy <some-mail-address>"]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
